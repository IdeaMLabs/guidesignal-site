# This directory holds sensitive service account credentials
# Add your Google service account JSON file here as: service-account.json
# This directory is tracked but the JSON file should be .gitignored